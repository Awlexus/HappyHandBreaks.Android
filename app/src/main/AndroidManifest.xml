<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.awlex.happyhandbreaks">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:name=".ExtendedApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="it.awlex.happyhandbreaks.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name="it.awlex.happyhandbreaks.AlarmReceiver"
            android:enabled="true"
            />

        <activity android:name="it.awlex.happyhandbreaks.ExerciseActivity" />
    </application>

</manifest>